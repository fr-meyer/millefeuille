# Millefeuille Artifact Index Fixtures

These fixtures exercise the read-only `millefeuille artifacts` and
`millefeuille status` commands. They contain no PDF payloads, provider payloads,
authenticated URLs, Zotero credentials, OCR output, or private paper text.
