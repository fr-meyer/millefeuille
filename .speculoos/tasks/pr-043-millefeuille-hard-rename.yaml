id: pr-043-millefeuille-hard-rename
title: Hard rename project identity to Millefeuille
status: implementation
branch: feat/pr-043-millefeuille-hard-rename
base_branch: dev
github:
  issue: null
  pull_request: null
  issue_project_item: null
  pull_request_project_item: null
  project_status: In Progress
  project_item: null
  url: null
  head_sha: null
  merge_commit: null
post_merge_expected:
  pull_request: 43
  base_branch: dev
  head_branch: feat/pr-043-millefeuille-hard-rename
  head_sha: merge-plan-exact-head
  merge_actor: mergeguez_dev_merge
  delete_branch_after_merge: true
scope:
  - rename the Python distribution, import package, module entry point, and console script to Millefeuille
  - remove the former package and CLI identity without compatibility shims or fallback aliases
  - migrate default lifecycle tags from the former queue namespace to the Millefeuille namespace
  - migrate OpenKB handoff and acceptance schema labels to `openkb-millefeuille-*`
  - rename fixture/test paths and documentation to the Millefeuille identity
  - update Speculoos metadata for the target `fr-meyer/millefeuille` repository identity
non_goals:
  - keeping old CLI commands, import packages, schema labels, or default tag names as supported aliases
  - live Zotero tag migration or collection writeback
  - PDF recovery, OCR/Mistral/PageIndex provider calls, OpenKB writes, or source-pack writes
  - GitHub remote repository slug rename unless performed later through an approved admin path
  - dev-to-main promotion, release tag, or package publication
acceptance:
  - "`python -m millefeuille --help` works and no old module entry point remains"
  - "installed console-script metadata exposes `millefeuille` and no old console command"
  - "source import paths use `millefeuille.*` only"
  - "default tags use `millefeuille`, `millefeuille-processed`, and `millefeuille-error`"
  - "handoff and acceptance schema labels use `openkb-millefeuille-*`"
  - "old package/CLI/import identity scan returns no tracked source hits"
  - "full tests, Ruff, metadata parse, and Speculoos validation pass"
validation:
  - ".venv/bin/python -m unittest discover -v"
  - ".venv/bin/ruff check ."
  - ".venv/bin/python -m millefeuille --help"
  - "ruby -ryaml -rjson -e 'Dir[\".speculoos/**/*.yaml\"].sort.each { |p| Psych.parse_file(p) }; Dir[\".speculoos/**/*.json\"].sort.each { |p| JSON.parse(File.read(p)) }; puts \"metadata ok\"'"
  - "git diff --check"
  - "speculoos status --json"
  - "speculoos actors --json"
  - "speculoos validate --task pr-043-millefeuille-hard-rename --validation \"git diff --check\" --json"
  - "old package/CLI/import identity scan"
evidence: []
publication_boundary:
  statement: "Hard rename/code-metadata PR only; no live Zotero writeback, PDF recovery, OCR/Mistral/PageIndex provider call, OpenKB write, source-pack write, credential change, remote GitHub repository rename, main-branch merge, release tag, or package publication. GitHub publication and dev-lane merge are allowed only after clean validation and exact-head review."
notes:
  - Franck explicitly rejected compatibility shims and fallback aliases for the old package/CLI/import identity.
  - Existing live Zotero items still carrying the pre-rename queue tag require a later write-key-enabled migration if they should enter the new default queue.
  - The remote GitHub repository slug and Mergeguez allowlist rename remain an admin-path step after this dev-lane code migration is merged.
